fastapi==0.110.1
uvicorn==0.25.0
python-dotenv==1.2.1
python-multipart==0.0.22
aiosqlite==0.19.0
websockets==15.0.1
pydantic==2.12.5

# Desktop streaming (enhanced)
aiortc==1.9.0
mss==9.0.1
numpy==1.26.4
pillow==10.3.0
pyautogui==0.9.54
av==12.0.0
bettercam  # Windows only
screeninfo==0.8.1
opencv-python

# Terminal support (Windows only - optional)
# pywinpty==2.0.13  # Not available on Linux ARM, handled with fallback

# Audio (Windows only - optional, will fallback if not available)
# pyaudiowpatch  # Not available on Linux, handled with try/except
